apiVersion: apps/v1
kind: Deployment
metadata:
  name: pst-apiweb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pst-apiweb
  template:
    metadata:
      labels:
        app: pst-apiweb
    spec:
      containers:
        - name: apiweb
          image: europe-west1-docker.pkg.dev/earnest-acre-474618-n9/toolshop/apiweb-k8s:e47f666
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: API_BACKEND_HOST
              value: api         # stays "api"
            - name: API_BACKEND_PORT
              value: "9000"
---
apiVersion: v1
kind: Service
metadata:
  name: apiweb          # ← was pst-apiweb
spec:
  type: ClusterIP
  selector:
    app: pst-apiweb
  ports:
    - name: http
      port: 80
      targetPort: 80